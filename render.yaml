services:
  - type: web
    name: spineguard-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api_integration.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.3